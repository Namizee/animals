{% extends 'base.html.twig' %}
{% block title %}Your animal!{% endblock %}
{% block body %}
        <section class="section">
                <div class="container">
                        <h3 class="title title-top">
                                {{ animal.title }}
                        </h3>
                        {{ include('forms/_delete_form.html.twig', {id: animal.id}, with_context = false) }}
                </div>
        </section>
{% endblock %}