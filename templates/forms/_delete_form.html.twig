<form action="{{ path('profile_animal_delete', {id: id}) }}" method="POST">
    <input type="hidden" name="_method" value="delete"/>
    <button type="submit" class="btn btn-block btn-danger">
       DELETE
    </button>
</form>
