{% extends 'base.html.twig' %}
{% block body %}
    <section class="section">
        <div class="container">
            <h3 class="title title-top">
                {{ animal.title }}
            </h3>
            <img src="{{ asset('build/images/' ~ animal.image) }}" alt="">
        </div>
    </section>
{% endblock %}